const add = (a,b) => a+b ;
const multiply = (a,b ) => a*b ;
const divide = (a,b) => a/b;
const sub = (a ,b ) => a - b ; 


module.exports = {
    add,
    multiply,
    divide,
    sub,
    
}

